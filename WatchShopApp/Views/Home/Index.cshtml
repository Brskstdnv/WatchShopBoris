<!DOCTYPE html>
<html lang="bg">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WatchShop - Винаги на време</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            overflow-x: hidden;
        }

        /* Стилизиране на секцията с изображението */
        .hero {
            position: relative;
            width: 100%;
            height: 80vh;
            background: url('https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcThL6zdH7to1EljtdSt7fKPMfd4TP0R2GXEFA&s') no-repeat center center/cover;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
        }

            .hero h1 {
                font-size: 4rem;
                font-weight: bold;
                color: white;
                background: rgba(0, 0, 0, 0.6);
                padding: 20px 40px;
                border-radius: 10px;
            }

        /* Контейнер за 360-градусовото изображение */
        .watch-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f8f9fa;
        }

            .watch-container img {
                max-width: 300px;
                width: 100%;
                transition: transform 0.1s linear;
            }
    </style>
</head>
<body>

    <!-- Голяма заглавна секция -->
    <section class="hero">
        <h1>Винаги на време</h1>
    </section>

    <!-- Секция за 360-градусово завъртане на часовник -->
   @* <section class="watch-container">
        <iframe src="https://clara.io/embed/0837e8ea-c1a9-45f6-ab97-f1ba06cbc027?renderer=webgl" width="800" height="600" allowfullscreen></iframe>
    </section>*@

    <script>
        document.addEventListener("scroll", function () {
            let scrollPosition = window.scrollY;
            let watch = document.getElementById("watch");
            watch.style.transform = `rotate(${scrollPosition}deg)`;
        });
    </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
